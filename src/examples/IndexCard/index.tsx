import CardLayout from "../../components/CardLayout";
import "./index.css";
interface IndexCardProps {}

function IndexCard(props: IndexCardProps) {
  return (
    <div className="index-card">
      <CardLayout layoutSettings={{}} />
    </div>
  );
}

export default IndexCard;
